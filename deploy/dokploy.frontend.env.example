# These values are injected at build time by GitHub Actions through Docker build args.
# They are not runtime environment variables in Dokploy.

VITE_API_BASE_URL=https://api.aura-ia.es/api/v1
VITE_DEFAULT_LOCALE=es
VITE_TURNSTILE_SITE_KEY=<turnstile-site-key>
VITE_DEV_MODE=false
